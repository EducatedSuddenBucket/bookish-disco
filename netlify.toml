[functions]
  node_bundler = "esbuild"
  directory = "."
[[redirects]]
  force = false
  from = "/*"
  status = 200
  to = "/.netlify/functions/index/:splat"

[build]
  command = "npm install serverless-http"
